from setuptools import setup

setup(name='univariate_one_step_forecasting',
      version='0.1',
      description='Module containing the configuration scripts, models and walk-forward for univariate one step forecasting',
      url='',
      author='J.Brownlee',
      author_email='',
      license='MIT',
      packages=['univariate'],
      zip_safe=False)
